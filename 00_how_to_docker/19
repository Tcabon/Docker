docker run -v $HOME/Abathur:/root --name Abathur -p 3000:3000 -d python:2-slim sh -c "pip install Flask && (cd /root && flask run --port=3000 --host=0.0.0.0)"

#creer le repos Abathur sur le HOME, et y mettre le app.py
#from flask import Flask
#app = Flask(__name__)

#@app.route("/")
#def hello():
#    return "Hello World!"
